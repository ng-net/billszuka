# CI-only test dependencies.
# The repo's main code uses only the Python stdlib (see tools/);
# this file exists so actions/setup-python@v5 cache: pip has a
# file to hash (otherwise it errors on first push).
#
# Pinned to compatible set: starlette<1.0 still uses httpx directly;
# starlette>=1.0 deprecates httpx in favor of httpx2 (which doesn't
# exist on PyPI yet). pytest.ini's `filterwarnings = error` then
# escalates the deprecation warning to a fatal collection error.
pytest>=8.0,<9.0
fastapi>=0.100,<0.115
httpx>=0.24,<0.28
starlette>=0.36,<0.41
